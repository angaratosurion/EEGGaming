using Godot;
using System;

public partial class Sky : Sprite2D
{
	// Called when the node enters the scene tree for the first time.
	public override void _Ready()
	{
		 
	}

	// Called every frame. 'delta' is the elapsed time since the previous frame.
	public override void _Process(double delta)
	{
		////Player.MOVEMENT;
		//Node2D  root=  this.Owner as Node2D;
		//	//this.GetNode<Node2D>("res://World/World");
		//GD.Print(root.Name);
		


		//Sprite2D player = root.GetNode<Sprite2D > ("Player");
		//GD.Print(player.Name);
		//Sprite2D mountain = new Mountain();
		//Vector2 pos = this.Position;
		//mountain.Position = new Vector2( 20000,  player.Position.Y + 2 * Player.MOVEMENT); 
		//root.AddChild(mountain);

		
	}
}
